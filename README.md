# Proyecto ESPACIOSDFR — TypeScript edition

Estructura mínima creada para convertir la página de `servicios.html` a TypeScript.

Comandos:

```
npm install
npm run build
```

Luego abrir `src/index.html` en el navegador (o copiar a `dist/` si prefieres servir `dist`).
